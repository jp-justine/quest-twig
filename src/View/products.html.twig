<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
<table>
{% for product in products|batch(3, 'No item') %}
    <tr>
        {% for column in product %}
            <td>{{ column }}</td>
        {% endfor %}
    </tr>
{% endfor %}
{{ dump(products) }}
</table>
</body>
</html>